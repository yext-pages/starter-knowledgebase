import { DirectAnswer as DirectAnswerData, Result } from '@yext/search-headless-react';
import { FeedbackType } from '../components/ThumbsFeedback';
import { DefaultRawDataType } from '../models';
/**
 * Analytics event types for cta click and title click.
 *
 * @public
 */
export declare type CardCtaEventType = 'CTA_CLICK' | 'TITLE_CLICK';
/**
 * The data types use to construct the payload in the analytics event.
 *
 * @public
 */
export declare type CardAnalyticsDataType<T = DefaultRawDataType> = DirectAnswerData | Result<T>;
/**
 * Analytics event types for interactions on a card.
 *
 * @public
 */
export declare type CardAnalyticsType = CardCtaEventType | FeedbackType;
export declare function useCardAnalytics<T>(): (cardResult: CardAnalyticsDataType<T>, analyticsEventType: CardAnalyticsType) => void;
//# sourceMappingURL=useCardAnalytics.d.ts.map
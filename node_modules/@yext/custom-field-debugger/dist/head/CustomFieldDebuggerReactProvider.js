import * as React from "react";
import * as ReactDOM from "react-dom";
import { init } from "./init";
const CustomFieldDebuggerReactProvider = (props) => {
  const { component, setupCFDebugger = true } = props;
  React.useEffect(() => {
    const rerenderFn = (props2, container = window.document.getElementById("root")) => {
      ReactDOM.render(
        React.createElement(component, { ...props2, setupCFDebugger: false }),
        container
      );
    };
    if (setupCFDebugger) {
      init(rerenderFn, window._RSS_PROPS_);
    }
  });
  return /* @__PURE__ */ React.createElement(React.Fragment, null, props.children);
};
export {
  CustomFieldDebuggerReactProvider
};

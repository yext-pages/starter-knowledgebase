const getRelativePrefixToRootFromPath = (path) => {
  const pathComponents = path.split("/");
  pathComponents.pop();
  return pathComponents.map((_) => "../").reduce((previousValue, currentValue) => previousValue + currentValue, "");
};
export {
  getRelativePrefixToRootFromPath
};

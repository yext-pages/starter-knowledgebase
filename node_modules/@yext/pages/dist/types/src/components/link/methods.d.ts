import { ReactNode } from "react";
import { LinkType, CTA } from "./types.js";
/**
 * Get the link from a CTA
 *
 * @param {CTA} cta
 *
 * @returns {string}
 */
declare const getHref: (cta: CTA) => string;
/**
 * CTA constructor
 *
 * @param {string} link
 * @param {LinkType} linkType
 * @param {string} label
 *
 * @returns {boolean}
 */
export declare function cta(link: string, linkType?: LinkType, label?: ReactNode): CTA;
/**
 * Checks if is a valid email address
 *
 * Regex defined in HTML Spec for <input type="email"> validation.
 * https://html.spec.whatwg.org/#email-state-(type=email)
 */
declare const isEmail: (string: string) => boolean;
/**
 * Reverse a string, used for obfuscation
 *
 * https://eddmann.com/posts/ten-ways-to-reverse-a-string-in-javascript/
 */
declare const reverse: (string: string) => string;
export { getHref, isEmail, reverse };
